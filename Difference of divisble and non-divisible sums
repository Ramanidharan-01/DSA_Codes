class Solution:
    def differenceOfSums(self, n: int, m: int) -> int:
        sum_1=0
        sum_2=0
        for i in range(1,n+1):
            if i%m!=0:
                sum_1+=i
            else:
                sum_2+=i
        return sum_1 - sum_2
