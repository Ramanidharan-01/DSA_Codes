class Solution:
    def segregateElements(self, arr):
        l=[]
        for i in arr:
            if i>=0:
                l.append(i)
        for i in arr:
            if i<0:
                l.append(i)
        arr[:]=l
        return arr
