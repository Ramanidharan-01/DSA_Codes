class Solution:
    def hasAlternatingBits(self, n: int) -> bool:
        last_bit=n&1
        while(n!=0):
            n>>=1
            if n&1==last_bit:
                return False
            last_bit=n&1
        return True
