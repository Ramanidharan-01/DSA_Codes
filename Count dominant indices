class Solution:
    def dominantIndices(self, nums: List[int]) -> int:
        n=len(nums)
        count=0
        for i in range(n-1):
            sum_=0
            for j in range(i+1,n):
                sum_+=nums[j]
            avg_=sum_/(n-i-1)
            print(avg_)
            if avg_<nums[i]:
                count+=1
        return count
        
